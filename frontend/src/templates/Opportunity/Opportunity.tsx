import { Base } from 'components/Base/Base';
import * as S from './Opportunity.styles';

export type OpportunityProps = {};

export const Opportunity = ({}: OpportunityProps) => {
  return (
    <Base>
      <S.Wrapper>Opportunity</S.Wrapper>
    </Base>
  );
};
