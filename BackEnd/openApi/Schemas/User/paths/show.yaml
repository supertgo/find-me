tags:
  - User
description: 'show specific user based on id'
security:
  - Authorization: [ ]

parameters:
  - $ref: '../parameters/UserId.yaml'
  - $ref: '../parameters/UserIncludesParameters.yaml'

responses:
  '200':
    $ref: '../responses/LoginSuccessResponse.yaml'