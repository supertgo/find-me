import { Base } from 'components/Base/Base';
import * as S from './Candidates.styles';

export type CandidatesProps = {};

export const Candidates = ({}: CandidatesProps) => {
  return (
    <Base>
      <S.Wrapper>Candidates</S.Wrapper>
    </Base>
  );
};
